import 'package:flutter/material.dart';

class VehicleUpload extends StatelessWidget {
  const VehicleUpload({super.key});

  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    throw UnimplementedError();
  }
}